# CSS Files Usage Report
## RealEstate Booking Suite Plugin

### ✅ CSS Files Currently IN USE (24 files)

#### Main Plugin Enqueued (in `realestate-booking-suite.php`):
1. **style.css** - Main stylesheet
2. **contact-widget.css** - Contact widget styles
3. **layouts.css** - Layout styles
4. **single-property-responsive.css** - Responsive styles for single property
5. **shortcodes.css** - Shortcode styles
6. **modern-dashboard.css** - Admin dashboard styles (admin only)
7. **rbs-archive.css** - Archive page styles

#### Feature-Specific Enqueued:
8. **archive.css** - Enqueued in `class-resbs-archive-handler.php` and `class-resbs-favorites.php`
9. **elementor.css** - Enqueued in `class-resbs-elementor.php`
10. **favorites.css** - Enqueued in `class-resbs-favorites.php`
11. **badges.css** - Enqueued in `class-resbs-badges.php`
12. **quickview.css** - Enqueued in `class-resbs-quickview.php`
13. **search-alerts.css** - Enqueued in `class-resbs-search-alerts.php`
14. **widgets.css** - Enqueued in `class-resbs-widgets.php`
15. **maps.css** - Enqueued in `class-resbs-maps.php`
16. **infinite-scroll.css** - Enqueued in `class-resbs-infinite-scroll.php`

#### Admin-Only Enqueued:
17. **maps-admin.css** - Enqueued in `class-resbs-maps.php` (admin)
18. **email-admin.css** - Enqueued in `class-resbs-email-manager.php` (admin)
19. **admin-dashboard.css** - Enqueued in `class-resbs-admin-dashboard.php` (admin)
20. **contact-admin.css** - Enqueued in `class-resbs-contact-settings.php` (admin)
21. **property-metabox.css** - Enqueued in `class-resbs-property-metabox.php` (admin)
22. **stunning-metabox.css** - Enqueued in `class-resbs-property-metabox.php` (admin)

#### Directly Loaded in Templates:
23. **single-property.css** - Loaded directly in `templates/single-property.php` (line 381)

#### Used via file_get_contents:
24. **email-styles.css** - Used in `class-resbs-email-manager.php` (read via file_get_contents for email templates)

---

### ❌ CSS Files NOT Currently Used (4 files)

1. **admin.css** - Contains admin styles but is NOT enqueued anywhere
   - Has styles for `.resbs-admin-wrap`, `.resbs-admin-header`, etc.
   - Possibly replaced by `admin-dashboard.css` or `modern-dashboard.css`

2. **input.css** - No references found in codebase
   - File appears to be unused

3. **tailwind.css** - No references found in codebase
   - Contains Tailwind CSS v4.1.14 compiled styles
   - Not enqueued anywhere

4. **single-property-override.css** - No references found in codebase
   - Contains override styles for theme conflicts
   - Not loaded in `single-property.php` template

---

### 📊 Summary

- **Total CSS files**: 28
- **Files in use**: 24 (86%)
- **Files not used**: 4 (14%)

### 💡 Recommendations

1. **admin.css** - Check if this should be enqueued or if it's been replaced by other admin CSS files
2. **input.css** - Consider removing if truly unused
3. **tailwind.css** - If Tailwind is not being used, this large file can be removed
4. **single-property-override.css** - Consider adding to `single-property.php` template if theme conflicts exist

---

*Report generated by analyzing wp_enqueue_style calls and direct CSS file references in the plugin codebase.*

